spring.application.name=learning_management_system
# postgres config
spring.datasource.url=jdbc:postgresql://localhost:5432/lms_db
spring.datasource.username=m.pourbafrani
spring.datasource.password=m.pourbafrani
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA/Hibernate configuration (optional, but commonly used)
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
# or create, create-drop, none
spring.jpa.hibernate.ddl-auto=update
# show SQL queries in console
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# JWT configuration
jwt.secret=MySuperSecretKey123456789
# 1 hour in milliseconds
jwt.expiration=3600000
# 7 days in milliseconds
jwt.refreshExpiration=604800000
